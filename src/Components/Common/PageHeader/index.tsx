import React, { FC } from "react";
import style from "../PageHeader/header.module.scss";

const PageHeader: FC = () => {
  return (
    <div className={style.asd}>
      <h1>page header should be here !!!</h1>
    </div>
  );
};

export default PageHeader;
